export const tagName = "get-random-from-form";

export enum EFormFields {
  VALUES = "values",
}

export enum classNames {
  RESULT = "result",
  RESULT_WRAPPER = "result-wrapper",
  RESULT_VALUE = "result-value",
}
