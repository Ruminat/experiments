export const mainStyles = `
  html, body {
    margin: 0;
    padding: 0;
    border: none;
    outline: none;

    --mainFont: 'Roboto', 'Fira Sans', 'Open Sans';
    --codeFont: 'Fira Code';

    font-family: var(--mainFont);
    box-sizing: border-box;
  }
`;
